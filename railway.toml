[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
buildContext = "tts-api"

[deploy]
startCommand = "python app.py"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

# Trigger deployment
